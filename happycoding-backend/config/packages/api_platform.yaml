api_platform:
    swagger:
         api_keys:
             JWT:
                name: Authorization
                type: header
    formats:
        json: ['application/json']
        jsonld: ['application/ld+json']
        jsonapi: ['application/vnd.api+json']
        html: ['text/html']
        csv: ['text/csv']
